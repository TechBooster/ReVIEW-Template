\geometry{top=14mm,bottom=16mm,left=5mm,right=5mm}

\definecolor{borderblue}{rgb}{0.2,0.4,0.9}

\hypersetup{%
  colorlinks,%
  %%urlcolor=borderblue,
  linkcolor=borderblue%
}

%% from review-pdfmaker
\usepackage{fancyhdr}
\usepackage{ulem}
%%\usepackage{supertabular}
\setlength{\headheight}{29pt}
\pagestyle{fancy}
\lhead{\gtfamily\sffamily\bfseries\upshape \leftmark}
\chead{}
\rhead{}
\cfoot{}
\rfoot{\thepage}
\renewcommand{\sectionmark}[1]{\markright{\thesection~#1}{}}
\renewcommand{\chaptermark}[1]{%
  \if@mainmatter
    \markboth{\prechaptername\ \thechapter\ \postchaptername~#1}{}
  \else
    \markboth{#1}{#1}
  \fi}
\renewcommand{\headfont}{\gtfamily\sffamily\bfseries}

\fancypagestyle{plainhead}{%
\fancyhead{}
\fancyfoot{} % clear all header and footer fields
\fancyfoot[R]{\thepage}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}}


\renewcommand{\reviewtitlefont}[0]{%
  \usefont{T1}{pag}{b}{n}\gtfamily\ebseries}
\renewcommand{\reviewmainfont}[0]{%
  \usefont{T1}{phv}{m}{n}\mgfamily}

\renewcommand{\textbf}[1]{%
  {\usefont{T1}{phv}{b}{n}\gtfamily\bfseries~#1}}

%%URLのフォントを通常のフォントにする
\urlstyle{same}

%%Helveticaを使う
\renewcommand{\sfdefault}{phv}

%%全体のデフォルトを丸ゴシック+Helveticaに
\renewcommand{\kanjifamilydefault}{\mgdefault}
\renewcommand{\familydefault}{\sfdefault}

%%色修正
\definecolor{shadecolorb}{gray}{0.5}

%%シングルクォート対応
\usepackage{upquote,textcomp}
\xspcode`'=0

%\renewcommand{\reviewcolumnhead}[2]{%
%{\noindent\large ■#2}}

%%framed.sty
%%//cmdや//emlistの横幅をぎりぎりまで広げる
\setlength{\FrameSep}{4pt}

%%table
\renewenvironment{reviewtable}[1]{%
  \begin{center}\scriptsize\setlength{\baselineskip}{1.5zw}
    \renewcommand\arraystretch{1.5}
    \begin{tabular}{#1}}{%
    \end{tabular}
  \end{center}}

\renewcommand{\reviewappendix}{%
\backmatter%
\fancyhead{}
}

\def\@makechapterhead#1{%
  \vspace*{0.5\Cvs}% 欧文は50pt
  {\parindent \z@ \raggedright \normalfont
    \ifnum \c@secnumdepth >\m@ne
      \if@mainmatter
        \hfill{\huge\headfont \@chapapp\thechapter\@chappos}
        \par\nobreak
        \vskip 0.5\Cvs % 欧文は20pt
      \fi
    \fi
    \interlinepenalty\@M
    \hfill{\huge \headfont #1\par\nobreak}
    \vspace*{-2mm}%
    \rule{\textwidth}{0.1mm}
    \vskip 1.0\Cvs}} % 欧文は40pt



\makeatletter%%

\long\def\@makecaption#1#2{{\small
  \advance\leftskip .0628\linewidth
  \advance\rightskip .0628\linewidth
  \vskip\abovecaptionskip
  \sbox\@tempboxa{\textgt{\textsf{#1}}\hskip1zw\relax \textgt{\textsf{#2}}}%
  \ifdim \wd\@tempboxa <\hsize \centering \fi
  \textgt{\textsf{#1}}\hskip1zw\relax \textgt{\textsf{#2}}\par
  \vskip\belowcaptionskip}}

\makeatother%% プリアンブルで定義する場合は必須(文字コードも)

\sloppy

%%目次と本文でページ番号を振り直しても表紙・中表紙ページと重複しないようRomanにしておく
\pagenumbering{Roman}

